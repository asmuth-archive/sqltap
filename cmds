
findOne(id)
findOne
findOneWhere(condition)

findAll
findSome(limit, [, offset [, order]])
findAllWhere(condition)
findSomeWhere(condition, limit, [, offset [, order]])

-------

findSingle(id, condition, order)
findMulti(limit, offset, condition, order)
